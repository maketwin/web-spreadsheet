/** Locked Excel-parity defaults for web-spreadsheet (P2 theme lock). */

/** Excel Calibri default on Windows / Microsoft 365. */
export const DEFAULT_FONT_SIZE = 11;

/** Default row / column geometry (px at 100% zoom) — matches coordinate.ts. */
export const DEFAULT_ROW_HEIGHT = 20;
export const DEFAULT_COL_WIDTH = 64;
